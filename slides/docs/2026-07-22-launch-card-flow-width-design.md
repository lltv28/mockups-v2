# Launch Card and Flow Width Design

## Communication job

By the end of slides 14 and 15, prospective clients should read the three visual examples and the process beneath them as one aligned system.

## Approved change

Expand the three-card row on both launch detail slides from a `900px` maximum width to `1060px`, matching the flowchart directly below it. Keep three equal card columns, the existing gaps, the square image previews, all copy, and the flowcharts unchanged.

## Responsive behavior

The existing mobile layout remains unchanged. Below `900px`, the cards continue stacking into one centered column and the flowchart continues stacking vertically.

## Verification

- Confirm both card rows and flowcharts resolve to the same width on desktop.
- Confirm both slides remain free of clipping or unintended overlap.
- Confirm the mobile stacked layout is unchanged.
- Confirm `slides/deck.html` remains unchanged.
